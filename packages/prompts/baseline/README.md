# Prompt Baseline

This directory preserves the Beta Feature Complete prompt baseline.

Prompts are versioned in Git and should be diffed against this baseline before RC changes.

Do not store prompts in the database.

Future breaking changes should be added under:

```text
packages/prompts/v2/
```
